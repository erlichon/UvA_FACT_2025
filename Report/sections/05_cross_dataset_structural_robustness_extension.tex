% Section 5: Cross-Dataset Structural Robustness (Extension 1)

\section{Cross-Dataset Structural Robustness}
\label{sec:cross_dataset_robustness}

A core promise of weight-based interpretability is that models learn meaningful geometric features rather than memorizing dataset-specific pixel correlations.
We test whether bilinear MLPs learn \textbf{Platonic forms}---e.g., ``0-ness'' as circularity---rather than idiosyncratic MNIST stroke patterns.

All models use \textbf{Center-of-Mass (CoM) normalization}: images are translated so intensity-weighted centroids align, forcing the bilinear layer to focus on \textbf{shape geometry} rather than absolute position.
We probe robustness under three distributional shifts:
(1) \textbf{Writer independence}: MNIST $\leftrightarrow$ EMNIST-Digits (same labels, different writers);
(2) \textbf{Domain transfer}: MNIST $\rightarrow$ USPS (different resolution/collection);
(3) \textbf{Geometric semantics}: MNIST $\rightarrow$ EMNIST-Letters, where letters are semantically different but geometrically similar (O$\to$0, I$\to$1, Z$\to$2, S$\to$5).

\subsection{Functional Analysis: Regularization Enables Transfer}

\subsubsection{Writer Independence: MNIST $\leftrightarrow$ EMNIST-Digits}

With the regularized CoM model, cross-dataset accuracy is near-perfect and symmetric:

\begin{table}[h]
\centering
\caption{Cross-Dataset Accuracy: MNIST $\leftrightarrow$ EMNIST-Digits (mean over 5 seeds).}
\label{tab:mnist_emnist_digits}
\small
\begin{tabular}{lc}
\toprule
\textbf{Direction} & \textbf{Accuracy} \\
\midrule
MNIST $\rightarrow$ EMNIST-Digits & 97.527 ± 0.024\% \\
EMNIST-Digits $\rightarrow$ MNIST & 97.910 ± 0.026\% \\
Bidirectional Average & 97.719 ± 0.012\% \\
\bottomrule
\end{tabular}
\end{table}

This confirms that regularized bilinear features \textbf{do not overfit to MNIST writers}.

\subsubsection{Domain Transfer: MNIST $\rightarrow$ USPS}

USPS introduces resolution and style shift. Regularization yields \textbf{+19 pp} improvement:

\begin{table}[h]
\centering
\caption{Cross-Dataset Accuracy: MNIST $\rightarrow$ USPS (mean over 5 seeds).}
\label{tab:usps}
\small
\begin{tabular}{lc}
\toprule
\textbf{Model} & \textbf{Accuracy} \\
\midrule
Baseline (no reg) & 53.42 ± 0.88\% \\
Regularized & 72.33 ± 0.83\% \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Geometric Semantics: MNIST $\rightarrow$ EMNIST-Letters}

We test whether MNIST classifiers ``see'' shape by running them on visually similar letters:

\begin{table}[h]
\centering
\caption{Letter$\to$Digit Transfer (regularized CoM model, 5 seeds).}
\label{tab:letter_digit}
\small
\begin{tabular}{lc}
\toprule
\textbf{Letter $\rightarrow$ Digit} & \textbf{Accuracy} \\
\midrule
O $\rightarrow$ 0 & 99.60 ± 0.05\% \\
I $\rightarrow$ 1 & 87.08 ± 0.10\% \\
Z $\rightarrow$ 2 & 88.75 ± 0.26\% \\
S $\rightarrow$ 5 & 87.38 ± 0.22\% \\
\bottomrule
\end{tabular}
\end{table}

The model's most confident prediction for each letter is \textbf{exactly} the geometrically corresponding digit (mapping accuracy = 100\%).

\subsection{Structural Analysis: Quadratic Form Similarity}

\subsubsection{The Failure of Cosine Similarity}

Direct eigenvector comparison via cosine similarity fails to distinguish geometrically similar pairs.
MNIST-0 vs EMNIST-O (similar) yields 0.358, while MNIST-0 vs EMNIST-X (dissimilar) yields 0.339---virtually identical.
The root cause: bilinear eigensystems share eigenvector directions while class information resides in eigenvalue \emph{weights}.

\subsubsection{Quadratic Form Similarity}

We propose comparing full decision surfaces. For class interaction matrices $A$ and $B$:
\begin{equation}
\mathrm{sim}(A,B) = \frac{\operatorname{tr}(A \cdot B)}{\|A\|_F \|B\|_F} \in [-1, 1]
\end{equation}
This incorporates both eigenvector alignment and eigenvalue magnitudes, with signs enabling discrimination (see Appendix~\ref{app:quadratic_form} for derivation).

\begin{figure}[t]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/extension2/extension2_quadratic_form_heatmap_digits.pdf}
        \caption{MNIST vs EMNIST-Digits}
        \label{fig:qf_digits}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/extension2/extension2_heatmap_quadratic_form.pdf}
        \caption{MNIST vs EMNIST-Letters}
        \label{fig:qf_letters}
    \end{subfigure}
    \caption{Quadratic Form Similarity ($k=20$).
    (a) Strong diagonal confirms writer-independent circuits.
    (b) Bright entries at shape-matched pairs (0--O, 1--I, 2--Z, 5--S) indicate mechanistically similar circuits; unrelated pairs are near zero or negative.}
    \label{fig:quadratic_heatmaps}
\end{figure}

Figure~\ref{fig:quadratic_heatmaps} shows clear discrimination.
Quadratic Form Similarity produces a \textbf{large gap} between similar (0.400) and dissimilar (-0.060) pairs ($p < 10^{-4}$).

\begin{table}[h]
    \centering
    \caption{Ranking analysis: Position of expected letter among 26 EMNIST letters sorted by Quadratic Form Similarity. Random baseline: rank 13.}
    \label{tab:ranking_analysis}
    \small
    \begin{tabular}{lccccc}
        \toprule
        & 0--O & 1--I & 2--Z & 5--S & Mean \\
        \midrule
        Rank & \textbf{1} & \textbf{2} & \textbf{1} & \textbf{1} & \textbf{1.2} \\
        \bottomrule
    \end{tabular}
\end{table}

Table~\ref{tab:ranking_analysis} confirms specificity: 3/4 pairs rank \#1, with mean rank 1.2 vs.\ random baseline of 13.

Figure~\ref{fig:similarity_vs_k} shows that similarity for shape-matched pairs increases and converges around $k=20$, while dissimilar pairs remain near zero throughout.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\linewidth]{figures/extension2/extension2_similarity_vs_k_quadratic_form.pdf}
    \caption{Quadratic Form Similarity vs number of eigenvectors $k$.
    Similar pairs converge around $k=20$; dissimilar pairs stay near zero with non-overlapping 90\% CIs.}
    \label{fig:similarity_vs_k}
\end{figure}

\subsection{Conclusion}

Regularized bilinear MLPs learn \textbf{transferable geometric circuits}: the ``0'' circuit is mathematically near-identical to the ``O'' circuit across independently trained models.
This strengthens the transparency claim---eigenvector features represent genuinely reusable shapes, not dataset-specific artifacts.
