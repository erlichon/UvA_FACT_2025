% Section 4: Reproduction Results

\section{Reproduction Results}
\label{sec:reproduction}

This section presents our reproduction of the main experiments from \citet{pearce2024bilinear}: Section 4 (Vision) on MNIST/Fashion-MNIST and Section 5 (Language) on bilinear transformers.

\subsection{Vision Results (Paper Section 4)}
\label{sec:vision_results}

We trained bilinear MLPs on MNIST and Fashion-MNIST under four regularization configurations across 5 random seeds each.

\subsubsection{Main Result: Regularization Induces Low-Rank Structure (Claim V1)}

The paper's central claim is that regularization induces low-rank structure in the bilinear interaction tensor.
Figure~\ref{fig:ablation_combined} confirms this through our ablation experiments.
Weight decay is the primary driver of low-rank structure, achieving the lowest effective rank (15.5 vs.\ 21.4 for full regularization), while noise augmentation increases effective rank but improves accuracy.
The effective rank ratio (15.5/38.5 $\approx$ 0.40) meets the paper's $<0.5$ criterion.
Eigenvalue distributions reveal sharp spectral decay with regularization (Figure~\ref{fig:eigenspectrum}, Appendix~\ref{app:vision_additional}), concentrating variance in 10--20 eigenvectors.
Top eigenvectors are interpretable digit-like patterns under regularization, while unregularized models show diffuse, noise-like patterns (Figures~\ref{fig:eigenvectors_noreg} and~\ref{fig:eigenvectors_reg}, Appendix~\ref{app:vision_additional}).

\begin{figure}[t]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/mnist_ablation.pdf}
        \caption{MNIST: Accuracy vs.\ effective rank}
        \label{fig:mnist_ablation}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/fashion_ablation.pdf}
        \caption{Fashion-MNIST: Accuracy vs.\ effective rank}
        \label{fig:fashion_ablation}
    \end{subfigure}
    \caption{Ablation study showing accuracy-interpretability trade-off across regularization configurations.
    Weight decay achieves the lowest effective rank on both datasets while maintaining competitive accuracy.
    The effective rank ratio (regularized/unregularized) meets the paper's $<0.5$ criterion.}
    \label{fig:ablation_combined}
\end{figure}

\subsubsection{Model Size and Truncation (Claim V5)}
\label{sec:model_size_truncation}

We confirm the paper's claim that bilinear MLPs maintain near-full accuracy with low-rank truncation: truncating to around 20 eigenvectors per class is sufficient even as model size increases (Figure~\ref{fig:app_truncation}).
Top eigenvectors are highly consistent across random seeds, especially for larger models.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/figure_5a_similarity.pdf}
        \caption{Eigenvector similarity across seeds}
        \label{fig:app_similarity}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/figure_5b_truncation.pdf}
        \caption{Truncation error by model size}
        \label{fig:app_truncation_error}
    \end{subfigure}
    \caption{Paper Figure 5 reproduction.
    \textbf{(a)} Top eigenvectors are highly consistent across random seeds, especially for larger models, with cosine similarity decreasing smoothly as eigenvector rank increases.
    \textbf{(b)} Truncation error decreases with model size; keeping $\sim$20 eigenvectors per class is sufficient for near-full accuracy.}
    \label{fig:app_truncation}
\end{figure}

\subsubsection{Additional Vision Results}
\label{sec:additional_vision_results}

We reproduce the original paper's Figures 4, 6, and 7; full visualizations are in Appendix~\ref{app:vision_additional}.
Increasing noise improves eigenvector interpretability while trading off with effective rank (Figure~\ref{fig:noise_effect}).
On binary similarity classification, the top eigenvector closely resembles the target digit (Figure~\ref{fig:challenge_task}), and eigenvector-derived masks cause significantly larger accuracy drops than random masks (Figure~\ref{fig:adversarial}), confirming V6.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/figure_4_noise_vs_rank.pdf}
        \caption{Effective rank increases with noise std}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \includegraphics[width=\linewidth]{figures/vision/figure_4_noise_vs_accuracy.pdf}
        \caption{Accuracy improves then saturates}
    \end{subfigure}
    \caption{Noise augmentation effect on effective rank and accuracy (reproducing paper Figure 4).
    Higher noise spreads variance across more eigenvectors while improving generalization.}
    \label{fig:noise_effect}
\end{figure}

\subsection{Language Results (Paper Section 5)}
\label{sec:language_results}

We reproduce the negation circuit discovery using pretrained bilinear transformers and publicly available SAEs from HuggingFace.\footnote{SAE repositories: \texttt{tdooms/fw-medium-scope}, \texttt{tdooms/ts-medium-scope}, \texttt{tdooms/fw-small-scope}.}

Our correlation analysis follows a consistent experimental protocol: tokenized text batches are passed through each model, we record \texttt{mlp\_in} and \texttt{mlp\_out} activations, encode \texttt{mlp\_out} with the output SAE to obtain sparse feature activations, and compare these to low-rank predictions derived from the interaction eigenpairs. We examine all output SAE features at the specified layer for each model, filtering to \emph{active features}---features with positive activation ($z_{\text{true}} > 0$) for at least one token position. For each active feature, correlations are computed over all available active samples (target-samples = all). Unless otherwise stated, we report Pearson correlation. Datasets follow model training sources: ts-medium uses TinyStories, fw-small and fw-medium use FineWeb-EDU.

\textbf{Critical finding: SAE availability gap.}
Inspection of the public SAE repositories revealed a limitation: \texttt{ts-medium-scope} (the paper's ts-tiny model) only provides \texttt{mlp-in} SAEs for layer 5, not layer 4.
Since Figure 8 analysis requires \emph{both} \texttt{mlp-in} and \texttt{mlp-out} SAEs, the paper's Figure 8 (ts-tiny, layer 4) cannot be reproduced with public artifacts.
We therefore use \texttt{fw-medium} (layer 7, expansion 8) which has both SAE types available.

\subsubsection{Negation Circuit Discovery (Paper Figure 8)}

We reproduce the AND-gate circuit discovery demonstrating how bilinear interactions implement semantic operations.
Feature 751 in fw-medium exhibits clear block structure in its interaction matrix (Figure~\ref{fig:negation_circuit}, Appendix~\ref{app:language_results}), separating input features into positive sentiment and negative concept clusters with opposing eigenvalues, creating AND-gate behavior.
Full methodology in Appendix~\ref{app:figure8_investigation}.

\subsubsection{Low-Rank Correlation Analysis (Paper Figure 9)}

The paper claims 69\% of features achieve $>$0.75 rank-2 correlation.
Our reproduction across three bilinear transformers (Figure~\ref{fig:correlation_analysis}, Appendix~\ref{app:language_results}) shows significantly lower values: mean rank-2 correlations range 0.27--0.51, with only 18--32\% exceeding the 0.75 threshold.
The discrepancy persists across all three models, suggesting an artifact quality gap rather than methodological error.

\subsubsection{Root Cause Analysis: SAE Training Duration}

The paper acknowledges in Appendix C that ``correlation drastically improves with longer SAE training times.''
We test this using fw-medium's layer 12 SAEs (expansion=16), where training-time checkpoints v0--v4 are publicly available (Figure~\ref{fig:sae_training_effect}, Appendix~\ref{app:language_results}).
Rank-2 correlation improves monotonically with SAE training duration (v0: 12\% $\to$ v4: 32\% above 0.75 threshold), but even the longest-trained public SAE reaches only 32\%, far below the paper's 69\% claim.
This confirms the methodology is sound; the reproducibility gap stems from SAE quality, not methodological flaws.

\subsection{Summary}

\textbf{Vision (Section 4): Fully Reproduced.} All claims (V1--V6) confirmed.

\textbf{Language (Section 5): Partially Reproduced.} AND-gate circuits (L1--L2) are confirmed with clear semantic structure.
Low-rank correlation (L3) falls short: public SAEs yield rank-2 correlations of 0.27--0.51, far below the claimed 69\% above 0.75.
Root cause analysis confirms correlation improves with longer SAE training, suggesting the paper used internal, better-trained SAEs not publicly available.
