% Section 2: Scope of Reproducibility

\section{Scope of Reproducibility}
\label{sec:scope}

We reproduce the main experimental sections of \citet{pearce2024bilinear}: Section 4 (Vision) demonstrating interpretable eigendecomposition on MNIST and Fashion-MNIST, and Section 5 (Language) discovering negation circuits in bilinear transformers using Sparse Autoencoder (SAE) analysis.

\subsection{Claims to Verify}

\paragraph{Section 4: Vision (Image Classification).}
The paper makes two main families of claims:
\begin{itemize}[leftmargin=*,topsep=0pt,itemsep=1pt]
    \item \textbf{Low-rank, interpretable structure:} Regularization induces low-rank interaction tensors whose top eigenvectors form digit-like templates rather than noise, with consistent spectra across seeds.
    \item \textbf{Robustness and controllability:} Noise and weight decay play complementary roles, enabling adversarial generation and truncation to a small number of eigenvectors without large accuracy loss.
\end{itemize}

\paragraph{Section 5: Language (Negation Circuits).}
For language, the paper claims that:
\begin{itemize}[leftmargin=*,topsep=0pt,itemsep=1pt]
    \item \textbf{SAE-based circuits:} SAEs reveal interpretable features implementing negation-like AND-gate circuits with opposing eigenvalues for ``not + positive'' vs.\ ``not + negative'' features.
    \item \textbf{Low-rank interactions across models:} A large fraction of SAE features admit high rank-2 correlation and sparse interaction matrices that generalize across model sizes.
\end{itemize}

\subsection{Success Criteria}

\textbf{Vision:} Effective rank ratio (regularized/unregularized) $< 0.6$; top eigenvectors visually resemble digit templates; test accuracy within 2\% of paper ($\sim$95--98\%); results consistent across 5 seeds.

\textbf{Language:} AND-gate circuits show opposing eigenvalue signs; $>$50\% of features show $>$0.75 rank-2 correlation (relaxed from 69\%); interaction matrices show interpretable block structure.

\subsection{Results Summary}

Table~\ref{tab:claims_summary} summarizes our reproduction outcomes: all six vision claims are fully reproduced, while language claims are partially reproduced---AND-gate circuits are confirmed but low-rank correlation falls short of the paper's 69\% threshold.

\begin{table}[h]
    \caption{Summary of reproduction results. \checkmark = reproduced, $\times$ = not reproduced, $\sim$ = partially reproduced.}
    \label{tab:claims_summary}
    \centering
    \small
    \begin{tabular}{lll}
        \toprule
        Claim & Description & Status \\
        \midrule
        \multicolumn{3}{c}{\textit{Vision (Section 4)}} \\
        \midrule
        V1 & Low-rank emergence & \checkmark \\
        V2 & Interpretable eigenvectors & \checkmark \\
        V3 & Overfitting without reg. & \checkmark \\
        V4 & Regularization roles & \checkmark \\
        V5 & Cross-seed consistency & \checkmark \\
        V6 & Adversarial generation & \checkmark \\
        \midrule
        \multicolumn{3}{c}{\textit{Language (Section 5)}} \\
        \midrule
        L1 & SAE feature discovery & \checkmark \\
        L2 & Negation/AND-gate circuits & \checkmark \\
        L3 & Low-rank interactions & $\sim$ \\
        L4 & Sparse interactions & \checkmark \\
        L5 & Cross-model generalization & $\sim$ \\
        \bottomrule
    \end{tabular}
\end{table}
