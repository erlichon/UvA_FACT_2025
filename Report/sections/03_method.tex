% Section 3: Methodology

\section{Methodology}
\label{sec:methodology}

\subsection{Bilinear Layer Architecture}

The bilinear layer replaces standard nonlinearities with element-wise multiplication:
\begin{equation}
    \mathbf{y} = (\mathbf{W}_l \mathbf{x}) \odot (\mathbf{W}_r \mathbf{x})
    \label{eq:bilinear}
\end{equation}
where $\mathbf{W}_l, \mathbf{W}_r \in \mathbb{R}^{d_{\text{hidden}} \times d_{\text{in}}}$ are learnable weight matrices and $\odot$ denotes the Hadamard product.
Unlike standard MLPs with fixed nonlinearities, bilinear layers compute \emph{input-dependent} gating: the left projection gates the right projection.
This creates a quadratic dependence on the input expressible as a third-order interaction tensor:
\begin{equation}
    y_c = \sum_{i,j} B_{cij} x_i x_j
    \label{eq:tensor}
\end{equation}
where $B_{cij} = \sum_h W_{\text{head},ch} (W_l)_{hi} (W_r)_{hj}$ and $W_{\text{head}} \in \mathbb{R}^{d_{\text{out}} \times d_{\text{hidden}}}$ is the output projection.

\subsection{Eigendecomposition for Interpretability}

For each output class $c$, we extract the $d_{\text{in}} \times d_{\text{in}}$ interaction matrix $B_c$ and symmetrize it:
\begin{equation}
    B_c^{\text{sym}} = \frac{1}{2}(B_c + B_c^\top)
\end{equation}
Symmetrization removes arbitrary asymmetry from the $W_l / W_r$ factorization while preserving the quadratic form $\mathbf{x}^\top B_c \mathbf{x}$.
The symmetric matrix admits eigendecomposition:
\begin{equation}
    B_c^{\text{sym}} = \sum_{r=1}^{d_{\text{in}}} \lambda_r^{(c)} \mathbf{v}_r^{(c)} (\mathbf{v}_r^{(c)})^\top
\end{equation}
where eigenvectors $\mathbf{v}_r^{(c)} \in \mathbb{R}^{d_{\text{in}}}$ represent interpretable input directions.
If the eigenspectrum is \emph{low-rank} (few dominant eigenvalues), the model's decision depends on only a few input directions that can be directly inspected.

We quantify spectral concentration using effective rank~\citep{roy2007effective}:
\begin{equation}
    \text{EffRank}(\boldsymbol{\lambda}) = \left( \frac{\|\boldsymbol{\lambda}\|_1}{\|\boldsymbol{\lambda}\|_2} \right)^2 = \frac{\left(\sum_i |\lambda_i|\right)^2}{\sum_i \lambda_i^2}
    \label{eq:effrank}
\end{equation}
where $\boldsymbol{\lambda} = (\lambda_1, \ldots, \lambda_d)$ is the vector of eigenvalues from the decomposition.
This scale-invariant measure ranges from 1 (single dominant eigenvalue) to $d$ (uniform spectrum).

\subsection{Language Model Analysis with SAEs}
\label{sec:language_method}

For language experiments, we combine bilinear decomposition with Sparse Autoencoders (SAEs)~\citep{bricken2023monosemanticity}.
SAEs decompose activations into sparse features via $\mathbf{z} = \text{TopK}(\mathbf{W}_{\text{enc}} \mathbf{h})$, where expansion factors of 4--8$\times$ disentangle superimposed concepts into monosemantic directions.

By placing SAEs at MLP input and output, we analyze how sparse input features interact to produce sparse outputs.
The bilinear structure implies $z^{\text{out}}_f \approx (\mathbf{z}^{\text{in}})^\top Q_f \, \mathbf{z}^{\text{in}}$, where $Q_f$ is the interaction matrix for output feature $f$.
Eigendecomposing $Q_f$ reveals dominant input directions---if rank-2 captures most variance, the feature depends on just two input directions, enabling circuit discovery.
We use pretrained SAEs with TopK sparsity ($k=30$); the paper claims ``most features'' achieve $>$0.75 rank-2 correlation.
We interpret ``most features'' as a median threshold ($>$50\% of features achieving $>$0.75 correlation).

\subsection{Datasets}
\label{sec:datasets}

\textbf{Vision Datasets.}
We use MNIST~\citep{lecun1998gradient} (60K train / 10K test, 28$\times$28 grayscale handwritten digits) and Fashion-MNIST~\citep{xiao2017fashion} (60K train / 10K test, 28$\times$28 grayscale clothing images).
Images are normalized to $[0,1]$ and flattened to 784-dimensional vectors.
For cross-dataset experiments (Section~\ref{sec:cross_dataset_robustness}), we additionally use EMNIST-Digits/Letters~\citep{cohen2017emnist} and USPS~\citep{hull1994database} (upscaled from 16$\times$16 to 28$\times$28).

\textbf{Language Datasets.}
The pretrained bilinear transformers use two datasets:
\textbf{TinyStories}~\citep{eldan2023tinystories} (2.1M synthetic children's stories, $\sim$470M tokens) for \texttt{ts-medium}, and
\textbf{FineWeb-EDU}~\citep{penedo2024fineweb} (educational web text subset of FineWeb, 1.3T tokens) for \texttt{fw-small} and \texttt{fw-medium}.
For correlation analysis (Figure~\ref{fig:correlation_analysis}), we use validation subsets: $\sim$1.57M tokens for ts-medium and $\sim$0.44M tokens for fw-medium due to computational constraints.

\subsection{Experimental Setup}

For vision experiments, we train bilinear MLPs on MNIST and Fashion-MNIST under four regularization configurations (Table~\ref{tab:reg_configs}), each with 5 random seeds (42--46).
Architecture: $d_{\text{hidden}} = 256$, 100 epochs, batch size 2048, AdamW optimizer with lr $10^{-3}$ and cosine annealing.
For language experiments, we analyze pretrained bilinear transformers (Table~\ref{tab:language_configs}, Appendix~\ref{app:language_results}).
Due to SAE availability constraints (see Section~\ref{sec:discussion}), our primary model is \texttt{fw-medium} rather than the paper's \texttt{ts-medium}.
We implemented a memory-efficient streaming algorithm for eigendecomposition to handle large interaction tensors.

\begin{table}[h]
    \caption{Regularization configurations (from paper Appendix G.1).}
    \label{tab:reg_configs}
    \centering
    \small
    \begin{tabular}{lcc}
        \toprule
        Configuration & Noise Std & Weight Decay \\
        \midrule
        \texttt{none} & 0.0 & 0.0 \\
        \texttt{noise} & 0.5 & 0.0 \\
        \texttt{wd} & 0.0 & 1.0 \\
        \texttt{full} & 0.5 & 1.0 \\
        \bottomrule
    \end{tabular}
\end{table}
