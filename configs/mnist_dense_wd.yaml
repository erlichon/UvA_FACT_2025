name: mnist_dense_wd
experiment_id: P1.3

model:
  mode: dense
  d_hidden: 256
  n_layer: 1

training:
  epochs: 100
  lr: 0.001
  batch_size: 2048

regularization:
  noise_std: 0.0
  weight_decay: 0.5

data:
  dataset: mnist
