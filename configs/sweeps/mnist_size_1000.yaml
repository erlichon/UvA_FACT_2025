name: mnist_size_1000
experiment_id: P1.5.6

model:
  mode: dense
  d_hidden: 1000
  n_layer: 1
  variance_corrected_init: false

training:
  epochs: 100
  lr: 0.001
  batch_size: 2048

regularization:
  noise_std: 0.5
  weight_decay: 1.0

data:
  dataset: mnist
