# MNIST Noise Sweep: noise_std = 0.5
# Part of Figure 4 reproduction - Effect of Input Noise
name: mnist_noise_0.5
experiment_id: sweep_noise_0.5

model:
  mode: dense
  d_hidden: 256
  n_layer: 1
  variance_corrected_init: false

training:
  epochs: 100
  lr: 0.001
  batch_size: 2048

regularization:
  noise_std: 0.5
  weight_decay: 0.0  # Isolate noise effect

data:
  dataset: mnist
